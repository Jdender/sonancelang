struct NamedStruct {
    foo: Foo,
    bar: Bar,
}

type unique NamedStructDesugar = struct {
    foo: Foo,
    bar: Bar,
}

struct TupleStruct(Foo, Bar);

type unique TupleStructDegugar = struct (Foo, Bar);

union NamedEnum {
    Foo: Foo,
    Bar: Bar,
}

type unique NamedUnionDesugar = union {
    Foo: Foo,
    Bar: Bar,
}

union TupleUnion(Foo, Bar);

type unique TupleUnionDesugar = union (Foo, Bar);
